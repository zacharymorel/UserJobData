<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>UserJobData</title>
    <link rel="stylesheet" href="/style.css">
  </head>
  <body>
    <h1>User Directory</h1>
    <ul>
      {{#id}}
      <li>
        {{#ImageUrl}}
          <img src={{ImageUrl}} alt="suppose to be an avatar.">
        {{/ImageUrl}}
        {{^ImageUrl}}
        <img src="/Profile-placeholder-450x450.png" alt="Image of Robot suppose to go here.">{{/ImageUrl}}
        <a href="/robots/{{id}}/">{{UserName}}</a>
        {{#Job}}
          <p>{{Job}}, {{Company}}</p>
        {{/Job}}
        {{^Job}}
          <p class="forHire">Available for hire</p>
        {{/Job}}
      </li>
      {{/id}}
    </ul>
  </body>
</html>
